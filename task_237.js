const str = "Hey JS! YOU R Amazing";
const vawels = ["a", "e", "i", "o", "u"];
function countVowels(data) {
  //let count = 0;
  // count = str.toLowerCase().split("").forEach((ch)=>{
  //     vawels.includes(ch) && count ++;
  // })
  //return count
}
const numOfVowels = countVowels(str);
console.log(numOfVowels);
